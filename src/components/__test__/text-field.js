import TextField from 'components/text-field';

describe('TextField component', () => {

    describe('when is rendered with default props', () => {

        it('should have default properties', () => {

        });

        it('should have default class', () => {

        });
    })
});

const SECOND = 1000;
const MINUTE = 60 * SECOND;
const HOUR = 60 * MINUTE;
const DAY = 24 * HOUR;
const MONTH = 30 * DAY;
const YEAR = 12 * MONTH;

module.exports = {
    SECOND,
    MINUTE,
    HOUR,
    DAY,
    MONTH,
    YEAR
};